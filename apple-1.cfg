MEMORY {
    RAM0: start = $0000, size = $0023, type = rw;
    WOZVARS: start = $0024, size = $002B, type = ro;
    RAM1: start = $002C, size = $00FF, type = rw;
    STACK: start = $0100, size = $01FF, type = ro;
    KBDIN: start = $0200, size = $027F, type = ro;
    RAM2: start = $0280, size = $0FFF, type = rw;
    ACIW: start = $C028, size = $C028, type = ro;
    ACIROM: start = $C100, size = $C1FF, type = ro;
    PIA: start = $D010, size = $D013, type = ro;
    RAM3: start = $E000, size = $EFFF, type = rw;
    WOZMON: start = $FF00, size = $FFFF, type = ro;
}

SEGMENTS {
    CODE: load = "RAM2", type = rw;
}
